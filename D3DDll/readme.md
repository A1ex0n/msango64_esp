### msg单人坐标获取

#### 需要逆向的数据

1.全体对象数组，数组以ID排列，ID自增

2.对局基址

3.通用rcx

4.pet移动call

5.hook地址/人机局hook地址

6.获取z坐标的call

#### 原理

1、hook pet对象下，目的坐标属性写入的地方，获取到pet移动到的xy坐标，通过getz获取到z坐标

2、循环调pet移动call，实时更新pet目的坐标，该坐标即为对方坐标，在大小地图中绘制pet目的坐标，即可实现esp